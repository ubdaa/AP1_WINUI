<Page
    x:Class="AP1_WINUI.Visiteurs.FicheFrais"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AP1_WINUI.Visiteurs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource AutoSuggestBackgroundThemeBrush}" Padding="50,50,50,50">
        <StackPanel>
            <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left">
                <TextBlock x:Name="Title" HorizontalAlignment="Left" TextWrapping="Wrap" Text="Fiche frais" VerticalAlignment="Top" FontSize="72" />
                <TextBlock x:Name="SubTitle" HorizontalAlignment="Left" TextWrapping="Wrap" Text="10 mai 2024 - 9 Juin 2024" VerticalAlignment="Top" FontSize="48" />
                <TextBlock x:Name="Name" HorizontalAlignment="Left" TextWrapping="Wrap" Text="Pour Saff" VerticalAlignment="Top" FontSize="24" FontStyle="Italic" />
            </StackPanel>
            <StackPanel Margin="0,45,0,0">
                <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="Frais forfaits" VerticalAlignment="Top" FontSize="30" />
                <CommandBar VerticalAlignment="Top" HorizontalAlignment="Left">
                    <CommandBar.PrimaryCommands>
                        <AppBarButton Icon="Add" Label="Ajouter Note" Click="AppBarButton_Click"/>
                        <AppBarSeparator />
                        <AppBarButton Icon="Edit" Label="Editer"/>
                        <AppBarButton Icon="Delete" Label="Supprimer"/>
                        <AppBarSeparator />
                    </CommandBar.PrimaryCommands>
                </CommandBar>
                <controls:DataGrid x:Name="datagridForfait" Height="200" AutoGenerateColumns="True" Margin="0,5,0,0" AutoGeneratingColumn="datagridForfait_AutoGeneratingColumn" />
            </StackPanel>
            <StackPanel Margin="0,45,0,0">
                <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="Frais hors forfaits" VerticalAlignment="Top" FontSize="30" />
                <controls:DataGrid x:Name="datagridHorsForfait" Height="200" AutoGenerateColumns="True" Margin="0,5,0,0" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
