<Page
    x:Class="AP1_WINUI.Visiteurs.ListeFiches"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AP1_WINUI.Visiteurs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Loaded="Page_Loaded">

    <ScrollViewer>
        <Grid Background="{ThemeResource AutoSuggestBackgroundThemeBrush}" Padding="50,50,50,50">
            <StackPanel>
                <TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="Liste fiches" VerticalAlignment="Top" FontSize="72" />
            </StackPanel>
            <StackPanel Padding="0,100,0,0">
                <ListView x:Name="ListFiches">
                    <!-- <Rectangle Height="1" Fill="LightGray" />
                    <ListViewItem>
                        <StackPanel>
                            <TextBlock Text="Fiche" FontSize="24" />
                            <TextBlock Text="etat" FontSize="18" />
                        </StackPanel>                    
                    </ListViewItem>-->
                </ListView>
            </StackPanel>
        </Grid>
    </ScrollViewer> 
</Page>
